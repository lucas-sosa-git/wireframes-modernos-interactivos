# Scripts

This skill currently does not require executable scripts.

A future version may include a `resolve-view-context` script that reads `manifests/agent-view-context.json` and returns direct dependencies for a target view.
